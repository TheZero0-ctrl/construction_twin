<% content_for :title, "New project" %>

<section class="space-y-6">
  <div class="space-y-3">
    <div class="space-y-2">
      <h1 class="text-3xl font-semibold tracking-tight text-stone-950 sm:text-4xl">Create a project</h1>
    </div>
  </div>

  <%= render "form", project: @project %>
</section>
