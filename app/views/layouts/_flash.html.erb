<div id="flash">
  <% flash.each do |type, message| %>
    <% next if message.blank? %>

    <div class="mb-6 rounded-2xl border px-4 py-3 text-sm font-medium shadow-sm <%= flash_tone_classes(type) %>">
      <%= message %>
    </div>
  <% end %>
</div>
